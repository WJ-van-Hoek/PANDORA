/**
 * The {@code com.pandora.controllers.requestbodies} package contains classes and interfaces related to request bodies
 * used in PANDORA for cell-based calculations. These request bodies are designed to encapsulate the necessary data
 * required for various calculations and are used as inputs to the corresponding services. The package includes the
 * following service interfaces:
 * The main interfaces in this package include:
 * <ul>
 * <li>{@link com.pandora.controllers.requestbodies.RequestBody RequestBody}:
 * <p>
 * An interface that defines the contract for validating request bodies.</li>
 * </ul>
 * <p>
 * The primary class in this package is:
 * <ul>
 * <li>{@link com.pandora.controllers.requestbodies.CellCalculationRequest CellCalculationRequest}:
 * <p>
 * A class representing a request body for cell-based calculations. It implements the
 * {@link com.pandora.controllers.requestbodies.RequestBody RequestBody} interface and includes validation logic for the
 * incoming data.</li>
 * </ul>
 */
package com.pandora.controllers.requestbodies;
