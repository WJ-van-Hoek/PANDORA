package com.pandora.calculation.services;

/**
 * An interface representing a service for performing calculations.
 * Implementations of this interface define the logic for specific calculations.
 */
public interface CalculationService {

    /**
     * Performs the calculation defined by the implementation.
     * The method signature may vary depending on the specific calculation being performed.
     */
    void calculate();
}
