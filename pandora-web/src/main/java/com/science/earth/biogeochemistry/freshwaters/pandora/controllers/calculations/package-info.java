/**
 * This package contains controllers related to calculations services in the Pandora application. The controllers handle
 * HTTP requests for different aspects of the calculations service.
 *
 * The {@link CalculationsServiceController} class handles GET requests for the calculations service index page. It maps
 * to the "/calculations-index.html" endpoint and returns the Thymeleaf template path for the index page.
 *
 * The {@link LaunchCalculationServiceController} class handles GET requests for launching the calculation service. It
 * maps to the "/launch-calculation-service.html" endpoint and returns the Thymeleaf template path for launching the
 * service.
 */
package com.pandora.controllers.calculations;
